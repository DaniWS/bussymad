---
import Header from './Header.astro';
import type { Locale } from '../lib/i18n';
import { appPath, faqPath } from '../lib/i18n';
import { GITHUB_REPO_URL, t } from '../lib/translations';

interface Props {
  locale: Locale;
  currentPath: string;
}

const { locale, currentPath } = Astro.props;
const strings = t(locale);
const href = appPath(locale);
---

<div class="site">
  <Header locale={locale} currentPath={currentPath} surface="site" />

  <main class="page" id="main-content">
    <div class="hero">
      <span class="hero__mark" aria-hidden="true">
        <img src="/logo.png?v=2" alt="" width="179" height="179" />
      </span>
      <h1>{strings.landingH1}</h1>
      <p class="hero__lede">{strings.landingLede}</p>
      <a class="hero__cta" href={href}>{strings.openApp}</a>
      <p class="hero__hint">{strings.landingHint}</p>
    </div>

    <section class="demo" aria-label={strings.demoLabel}>
      <figure class="demo__item">
        <video
          class="demo__media demo__media--video"
          width="560"
          height="490"
          autoplay
          muted
          loop
          playsinline
          preload="none"
          poster="/demo/hourly-poster.jpg"
          aria-label={strings.demoHourlyAlt}
        >
          <source src="/demo/hourly.webm" type="video/webm" />
        </video>
        <figcaption class="demo__caption">
          <strong>{strings.viewHourly}</strong>
          <span>{strings.demoHourlyCaption}</span>
        </figcaption>
      </figure>

      <figure class="demo__item">
        <img
          class="demo__media"
          src="/demo/rating.png"
          width="345"
          height="269"
          alt={strings.demoRatingAlt}
          loading="lazy"
          decoding="async"
        />
        <figcaption class="demo__caption">
          <strong>{strings.viewDayPulse}</strong>
          <span>{strings.demoRatingCaption}</span>
        </figcaption>
      </figure>
    </section>

    <p class="landing__source">
      {strings.landingSource}
      {' '}
      <a href={GITHUB_REPO_URL} target="_blank" rel="noopener noreferrer">{strings.landingSourceLink}</a>
      {' · '}
      <a href={faqPath(locale)}>{strings.landingFaqLink}</a>
    </p>
  </main>
</div>

<style>
  .site {
    display: flex;
    flex-direction: column;
    min-height: 100dvh;
  }

  .page {
    display: flex;
    flex-direction: column;
    flex: 1;
    width: min(54rem, calc(100% - 2rem));
    margin: 0 auto;
    padding: 2.5rem 0 3.5rem;
  }

  .hero {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    width: min(36rem, 100%);
  }

  .hero__mark {
    display: grid;
    place-items: center;
    width: 3.25rem;
    height: 3.25rem;
    margin-bottom: 1.25rem;
    border-radius: 0.85rem;
    background: #1a7f5a;
    overflow: hidden;
  }

  .hero__mark img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  h1 {
    margin: 0 0 0.85rem;
    font-size: clamp(2rem, 6vw, 3rem);
    font-weight: 700;
    letter-spacing: -0.04em;
    line-height: 1.1;
  }

  .hero__lede {
    margin: 0 0 1.5rem;
    font-size: 1.0625rem;
    line-height: 1.55;
    color: var(--ink-muted);
    white-space: pre-line;
  }

  .hero__cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.7rem 1.35rem;
    border-radius: 999px;
    background: var(--accent);
    color: var(--paper);
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
  }

  :global(html[data-theme='dark']) .hero__cta {
    color: #0f1210;
  }

  .hero__cta:hover,
  .hero__cta:focus-visible {
    filter: brightness(1.06);
  }

  .hero__hint {
    margin: 0.85rem 0 0;
    font-size: 0.8125rem;
    color: var(--ink-muted);
  }

  .demo {
    display: grid;
    grid-template-columns: auto auto;
    justify-content: start;
    align-items: start;
    gap: 1rem 1.25rem;
    margin: 2.25rem 0 0;
    padding: 0;
  }

  .demo__item {
    display: flex;
    flex-direction: column;
    gap: 0.65rem;
    margin: 0;
    width: fit-content;
    max-width: 100%;
  }

  .demo__media {
    display: block;
    height: clamp(13.5rem, 36vw, 20.5rem);
    width: auto;
    max-width: 100%;
    object-fit: contain;
    object-position: left top;
    border-radius: 0.75rem;
    border: 1px solid var(--hairline);
    background: #0f1418;
  }

  .demo__media--video {
    pointer-events: none;
  }

  .demo__caption {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
    margin: 0;
    max-width: 28rem;
    font-size: 0.8125rem;
    line-height: 1.45;
    color: var(--ink-muted);
  }

  .demo__caption strong {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--ink);
  }

  .landing__source {
    margin: 1.5rem 0 0;
    max-width: 36rem;
    font-size: 0.8125rem;
    line-height: 1.5;
    color: var(--ink-muted);
  }

  .landing__source a {
    color: var(--accent);
    text-decoration: underline;
    text-underline-offset: 0.12em;
  }

  .landing__source a:hover,
  .landing__source a:focus-visible {
    filter: brightness(1.08);
  }

  @media (max-width: 560px) {
    .demo {
      grid-template-columns: 1fr;
    }

    .demo__item {
      width: 100%;
    }

    .demo__media {
      width: 100%;
      height: auto;
      aspect-ratio: 560 / 490;
      object-fit: cover;
    }
  }
</style>
